# ElectricSignal interface - electrical signal with reference
# Standard library interface for analog and generic signals

import Electrical, ElectricPower

interface ElectricSignal:
    # Signal line
    line = new Electrical
    # Power reference for the signal
    reference = new ElectricPower
